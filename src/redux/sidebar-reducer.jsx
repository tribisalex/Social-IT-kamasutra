
let initialState = {
    sidebars: [
        {id: 1, friend: 'Sahmen'},
        {id: 2, friend: 'Armen'},
        {id: 3, friend: 'Alan'}
    ]
}

const sidebarReducer = (state=initialState, action) => {

    return state;
}

export default sidebarReducer;